{
  "name": "key-management",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "gradle build -x test",
        "cwd": "{projectRoot}"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "gradle test",
        "cwd": "{projectRoot}"
      }
    }
  }
}
